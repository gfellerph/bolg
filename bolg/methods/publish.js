const firebase = require('../config/firebase');
const helpers = require('../helpers');
const webpackManifest = require('../config/webpack.manifest.json');

module.exports = function publish(id) {

};
